services:
  - type: web
    name: xenoglossia-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y tesseract-ocr
    startCommand: uvicorn backend:app --host 0.0.0.0 --port $PORT